extension_dir = "ext"

; Database Drivers
extension=pdo_mysql
extension=mysqli

; String Handling (Fixes the current error)
extension=mbstring

; General Compatibility (Prevents future errors)
extension=openssl
extension=curl
extension=fileinfo
